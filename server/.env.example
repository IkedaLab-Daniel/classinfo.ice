# ClassInfo Server Environment Variables

# Database
MONGO_URI=mongodb://localhost:27017/classinfo

# Server Configuration
NODE_ENV=development
PORT=5001

# Rate Limiting
RATE_LIMIT_WINDOW_MS=300000
RATE_LIMIT_MAX_REQUESTS=100

# Email Configuration (for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
CONTACT_EMAIL=admin@classinfo.com

# Web Push Notifications (VAPID Keys)
# Generate these using: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key

# Client URL (for email links)
CLIENT_URL=http://localhost:5173

# Optional: For production
# CORS_ORIGINS=https://your-domain.com,https://your-netlify-app.netlify.app
